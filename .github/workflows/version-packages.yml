name: Version Packages

on:
  push:
    branches:
      - main
    paths:
      - '.nx/version-plans/**'

permissions:
  contents: write
  pull-requests: write

jobs:
  version:
    name: Create or Update Release PR
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Upgrade npm
        run: npm install -g npm@11

      - name: Install Dependencies
        run: npm ci

      - name: Check for Version Plans
        id: check
        run: |
          if [ -d ".nx/version-plans" ] && [ "$(ls -A .nx/version-plans/*.md 2>/dev/null | wc -l)" -gt 0 ]; then
            echo "has-plans=true" >> $GITHUB_OUTPUT

            PLAN_COUNT=$(ls -1 .nx/version-plans/*.md 2>/dev/null | wc -l)
            echo "plan-count=$PLAN_COUNT" >> $GITHUB_OUTPUT

            echo "📦 Found $PLAN_COUNT version plan(s)"
          else
            echo "has-plans=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No version plans found"
          fi

      - name: Configure Git
        if: steps.check.outputs.has-plans == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Preview Version Changes
        if: steps.check.outputs.has-plans == 'true'
        id: preview
        run: |
          echo "🔍 Previewing version changes..."
          npx nx release version --dry-run 2>&1 | tee /tmp/release-preview.txt || true

      - name: Detect Prerelease
        if: steps.check.outputs.has-plans == 'true'
        id: detect-prerelease
        run: |
          # Check if any version plan contains prerelease keywords
          if grep -qE "':(prepatch|preminor|premajor|prerelease)" .nx/version-plans/*.md 2>/dev/null; then
            echo "is-prerelease=true" >> $GITHUB_OUTPUT
            echo "🧪 Detected prerelease version plan"
          else
            echo "is-prerelease=false" >> $GITHUB_OUTPUT
            echo "🎯 Detected stable version plan"
          fi

      - name: Apply Version Plans
        if: steps.check.outputs.has-plans == 'true'
        id: release
        run: |
          echo "📦 Applying version plans..."

          # Apply version plans (updates pom.xml/package.json, generates CHANGELOG, deletes version plans, commits)
          # Note: Nx MUST commit to generate changelogs (needs git history)
          # We let Nx commit, then extract changes and reset for PR creation
          if [ "${{ steps.detect-prerelease.outputs.is-prerelease }}" = "true" ]; then
            echo "🧪 Applying with --preid rc"
            npx nx release version --preid rc
          else
            echo "🎯 Applying stable version"
            npx nx release version
          fi

          # Regenerate package-lock.json with updated workspace versions
          echo "🔄 Regenerating package-lock.json..."
          npm install --package-lock-only

          # Extract changed projects from the Nx commit
          CHANGED=$(git diff --name-only HEAD~1 | grep -E '(pom.xml|package.json)' | grep -E '(apps|infra)' | xargs -I {} dirname {} | sed 's|^apps/||' | sed 's|^infra/resources/[^/]*/||' | sort -u | tr '\n' ', ' | sed 's/,$//')

          echo "changed-projects=$CHANGED" >> $GITHUB_OUTPUT
          echo "📋 Changed projects: $CHANGED"

          # Reset the commit but keep all changes staged (including CHANGELOGs)
          # This allows create-pull-request action to create its own commit
          git reset --soft HEAD~1
          git add -A

          echo "✅ Changes prepared for PR (CHANGELOGs included)"

      - name: Get Version Summary
        if: steps.check.outputs.has-plans == 'true'
        id: summary
        run: |
          SUMMARY=""

          # Check apps (Maven projects with pom.xml)
          for pom_file in apps/*/pom.xml; do
            if [ -f "$pom_file" ]; then
              PROJECT=$(echo $pom_file | sed 's|apps/||' | sed 's|/pom.xml||')
              # Extract version from pom.xml
              VERSION=$(grep -A 1 "<artifactId>$PROJECT</artifactId>" "$pom_file" | grep "<version>" | sed 's/.*<version>\(.*\)<\/version>.*/\1/' | head -1)

              if [ -n "$VERSION" ]; then
                # Check if RC or stable
                if [[ $VERSION == *"-rc."* ]]; then
                  BADGE="🧪 RC"
                else
                  BADGE="🎯 Stable"
                fi

                SUMMARY="${SUMMARY}\n- **${PROJECT}**: \`${VERSION}\` ${BADGE}"
              fi
            fi
          done

          # Check infra projects (still use package.json)
          for file in infra/resources/*/*/package.json; do
            if [ -f "$file" ]; then
              # Extract project name from path: infra/resources/dev/onboarding-ms/package.json -> onboarding-ms
              PROJECT=$(basename $(dirname "$file"))
              VERSION=$(jq -r '.version' $file 2>/dev/null)

              if [ "$VERSION" != "null" ] && [ -n "$VERSION" ]; then
                # Check if RC or stable
                if [[ $VERSION == *"-rc."* ]]; then
                  BADGE="🧪 RC"
                else
                  BADGE="🎯 Stable"
                fi

                SUMMARY="${SUMMARY}\n- **${PROJECT}**: \`${VERSION}\` ${BADGE}"
              fi
            fi
          done

          echo "summary<<EOF" >> $GITHUB_OUTPUT
          echo -e "$SUMMARY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Pull Request Body
        if: steps.check.outputs.has-plans == 'true'
        id: pr-body
        run: |
          cat > /tmp/pr-body.md <<'EOFBODY'
          # 🦋 Release

          This PR was opened by the **Version Packages** workflow. When you're ready to do a release, merge this PR and packages will be released automatically.

          If you're not ready to release yet, that's fine! Whenever you add more version plans to `main`, this PR will be updated.

          ## 📦 Releases

          ${{ steps.summary.outputs.summary }}

          ## 📋 Version Plans Applied

          This release applies **${{ steps.check.outputs.plan-count }}** version plan(s).

          <details>
          <summary>📝 View Changed Projects</summary>

          ```
          ${{ steps.release.outputs.changed-projects }}
          ```

          </details>

          ---

          <details>
          <summary>ℹ️ About Version Plans</summary>

          Version plans allow you to declare the type of changes in your code:

          - **patch**: Bug fixes (1.0.0 → 1.0.1)
          - **minor**: New features (1.0.0 → 1.1.0)
          - **major**: Breaking changes (1.0.0 → 2.0.0)
          - **prepatch/preminor/premajor**: RC versions (1.0.0 → 1.1.0-rc.0)

          To create a version plan:
          ```bash
          npx nx release plan
          ```

          Multiple version plans are automatically aggregated, with the highest bump type winning.

          </details>
          EOFBODY

      - name: Create or Update Pull Request
        if: steps.check.outputs.has-plans == 'true'
        id: create-pr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: changeset-release/main
          base: main
          title: "chore: Release (Version Packages) 🦋"
          body-path: /tmp/pr-body.md
          labels: |
            release
            automated
          commit-message: "chore: version packages"
          delete-branch: false

      - name: No Version Plans Found
        if: steps.check.outputs.has-plans == 'false'
        run: |
          echo "ℹ️ No version plans found in .nx/version-plans/"
          echo "Create one with: npx nx release plan"

      - name: Job Summary
        if: steps.check.outputs.has-plans == 'true'
        run: |
          echo "### ✅ Version Packages PR Ready" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**PR Number**: #${{ steps.create-pr.outputs.pull-request-number }}" >> $GITHUB_STEP_SUMMARY
          echo "**Version Plans Applied**: ${{ steps.check.outputs.plan-count }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.summary.outputs.summary }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Merge the PR to release! 🚀" >> $GITHUB_STEP_SUMMARY
