{
  "name": "@selfcare/iam-ms",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/iam-ms",
  "projectType": "application",
  "tags": ["type:app", "stack:java"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mvn clean install -DskipTests",
        "cwd": "apps/iam-ms"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Deploy iam-ms'"
      }
    }
  }
}
