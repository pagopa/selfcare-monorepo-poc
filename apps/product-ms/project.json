{
  "name": "@selfcare/product-ms",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/product-ms",
  "projectType": "application",
  "tags": ["type:app", "stack:java"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mvn clean install -DskipTests",
        "cwd": "apps/product-ms"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Deploy product-ms'"
      }
    }
  }
}
