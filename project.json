{
  "name": "it.pagopa.selfcare:root",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "nx-release-publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Skipping publish for root project'"
      }
    }
  }
}
